import pandas as pd 

table = pd.read_csv('flights_task.csv')
cells = pd.DataFrame(table)

#print(cells)
state_fligths = cells.groupby(['STATE'], as_index=False)['DEP'].agg(['max', 'min', 'sum'])
state_fligths['DIF'] = (state_fligths['max'] - state_fligths['min'])
state_fligths = state_fligths.sort_values(by='DIF', ascending=False)
print(state_fligths['STATE'].iloc[0])




























































































































































































































































































